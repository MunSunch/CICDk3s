apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cicdk3s-ingress
  namespace: spring
  labels:
    name: cicdk3s-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: service-advice.ru
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: cicdk3s-service
            port: 
              number: 8080
